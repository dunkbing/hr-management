services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hr_management
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12052003
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build: ./hr-management
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/hr_management
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: 12052003
    volumes:
      - lucene-data:/app/lucene-index
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./front-end-master
    ports:
      - "5173:5173"
    depends_on:
      - api

volumes:
  pgdata:
  lucene-data:
